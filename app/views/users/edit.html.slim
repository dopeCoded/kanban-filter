.container
  .row.justify-content-center
    .col-md-6
      h1.text-center.mb-4 ユーザー情報の編集
      = form_with(model: @user, url: user_path(@user), method: :patch, local: true, class: 'needs-validation', novalidate: true) do |form|
        - if @user.errors.any?
          #error_explanation.alert.alert-danger
            h2
              = pluralize(@user.errors.count, "error")
              |  prohibited this user from being saved:
            ul
              - @user.errors.full_messages.each do |message|
                li = message
        .mb-3
          = form.label :name, class: 'form-label'
          = form.text_field :name, class: 'form-control'
        .mb-3
          = form.label :email, class: 'form-label'
          = form.email_field :email, class: 'form-control'
        .mb-3
          = form.label :password, "新しいパスワード", class: 'form-label'
          = form.password_field :password, class: 'form-control'
        .mb-3
          = form.label :password_confirmation, "新しいパスワード（確認用）", class: 'form-label'
          = form.password_field :password_confirmation, class: 'form-control'
        .text-center
          = form.submit "更新する", class: "btn btn-primary"
      .text-center.mt-4
        = link_to 'トップページへ戻る', root_path, style: "color: green;", class: "btn btn-link"
